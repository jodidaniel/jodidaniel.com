# cms-platform lock — the platform release this site is pinned to.
# Bumped by the platform-bump workflow, which moves this file, the theme gem
# and every uses:@ pin as one atomic PR; Dependabot is set to ignore every
# cms-platform reference — the gem (cms-platform#242) and the uses:@ pins
# (cms-platform#244) alike (see .github/dependabot.yml). See the platform's
# docs/SYNC.md.
platform_repo: Adam-S-Daniel/cms-platform
platform_ref: v0.1.90
